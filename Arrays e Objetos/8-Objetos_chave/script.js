let pessoa = {
    nome: "Lucas",
    idade: 20,
    sexo: "Masculino"
}
console.log(pessoa); // imprime o objeto pessoa

console.log(Object.keys(pessoa)); // imprime SOMENTE as propiedades(chaves) do objeto